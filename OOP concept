<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Opps concept</title>
</head>
<body>
    <script>
        /*
       let item = {
                    name:'phone',
                    price:25000,
                    quantity:1,
                    categories:['electronics','phones'],
                    dimensions:{
                        length:7,
                        breadth:3.5,
                        height:.5
                    }
                    }

      console.log(item.name);
      console.log(item.quantity); 
      console.log(item.dimensions.breadth);
      console.log(item.categories[1]);   
      
      let item2 = new Object();
      item2.name = "Arul";
      item2.price = 250000;
      item2.quantity =  3;
    //   console.log(item2);
    //   console.log(item);

      let key = 'price';
      item[key]= 2800000;
      console.log(item);

      let key_name = "name";
      item2[key_name]="kavi";
      console.log(item2);

      let key_price =330000;
      item2[key_price]=220000;
      console.log(item2);

      item2.categories =['mobiles','laptops','telivitions','refregirators','home afflains'];
      console.log(item2);
      console.log(item2.categories[3]); 

      item = {
            name: "phone", 
            quantity:1,
            price:25000,
            buy: function(){
                console.log('item added to cart')
            },
            addToList(){
                console.log('item added to list')
            }
            }
        console.log(item.name);
        console.log(item.addToList());
        console.log(item.buy());
        console.log(item.price);
        console.log(item.quantity);
        console.log(item); 

        let user = new Object();
        user.name ="Kavi";
        user.age = 24;
        user.color = "black";
        user.dimensions={height :"6 ft",weight:"54kg"};
        user.favorites =['kkr','amma','appa','wife'];
        console.log(user);

        console.log(user.dimensions.height);
        console.log(user.dimensions.weight); */

         class User{
            static noOfUsers = 0;
            constructor(name,age){
                this.name = name;
                this.age = age;
                User.noOfUsers ++
            }
        login(){
            console.log("You are logged in", this.name)
        }
        logout(){
            console.log('You are logOut')
        }
         }

         let user1 =new User('Arul',23);
         let user2 =new User('Jeya',30);
         let user3 = new User('Kavi',22);
         let user4 = new User('Sneha',23);
         console.log(User.noOfUsers)
         console.log(user1.login())
      

    </script>
</body>
</html>
